package com.ecommerce.backend.service;

import com.ecommerce.backend.entity.HeroSection;

public interface HeroSectionService {
    HeroSection getHeroSection();
    HeroSection updateHeroSection(HeroSection heroSection);
}
